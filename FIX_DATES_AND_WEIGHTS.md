# 🔧 ИСПРАВЛЕНИЕ: Даты и Веса

**Дата:** 06.02.2026  
**Проблемы:** Invalid Date на iPhone + Веса не отображаются

---

## 🐛 ЧТО БЫЛО НЕ ТАК

### **Проблема 1: Invalid Date**
- Даты конвертировались в `YYYY-MM-DD` в `WorkoutDetails.jsx`
- НО в `assignedWorkoutsService` сохранялись только `dates`, без `weekData.days`
- При загрузке на iPhone `weekData` загружался из старого шаблона `workouts`
- Старый шаблон мог содержать даты в формате `DD.MM.YYYY`

### **Проблема 2: Веса не отображаются**
- В `CreateWorkout.jsx` веса добавлялись в `exerciseData.weight`
- НО при назначении тренировки сохранялись только `dates`, без `weekData.days`
- При загрузке на iPhone `weekData` загружался из старого шаблона `workouts`
- Старый шаблон НЕ содержал автозагруженные веса

---

## ✅ ЧТО ИСПРАВЛЕНО

### **Исправление 1: assignedWorkoutsService.js**
**Было:**
```javascript
// Сохраняли только ссылку
dates: weekData.dates || {},
// НЕ сохраняли weekData.days
```

**Стало:**
```javascript
// Сохраняем ПОЛНЫЕ данные недели
weekData: {
  weekNumber: weekData.weekNumber,
  days: weekData.days,        // ✅ Упражнения с весами
  dates: weekData.dates || {} // ✅ Даты в формате YYYY-MM-DD
}
```

### **Исправление 2: workouts.tsx (мобильное приложение)**
**Улучшена функция `getExerciseDetails()`:**
```typescript
// Ищем вес в разных форматах:
if (data.weight) {
  weight = data.weight; // ✅ Новый формат
} else if (exercise.numberTimes && data[exercise.numberTimes - 1]) {
  weight = data[exercise.numberTimes - 1]; // ✅ Формат массива
} else if (data['7']) {
  weight = data['7']; // ✅ Legacy формат
}
```

---

## 🧪 КАК ПРОТЕСТИРОВАТЬ

### **Шаг 1: Создать тренировку с весами**
1. Откройте sport_base
2. Перейдите к клиенту
3. Создайте новую тренировку
4. Добавьте упражнения (веса должны автоматически подтянуться)
5. **Откройте консоль браузера (F12)**
6. Проверьте логи:
   ```
   ✅ Автозагрузка веса для "Жим лежа": 80 кг (8 повторений)
   ```

### **Шаг 2: Проверить на странице WorkoutDetails**
1. Сохраните тренировку
2. Откройте её в WorkoutDetails
3. Убедитесь что веса отображаются: **3 × 8 (80)**

### **Шаг 3: Назначить тренировку**
1. Выберите даты для каждого дня
2. Нажмите "Отправить клиенту"
3. **Проверьте логи в консоли:**
   ```
   💾 Сохраняем полные данные недели с упражнениями и весами
   📦 weekData.days: 7 дней
   📅 weekData.dates: {monday: "2026-02-10", tuesday: "2026-02-11", ...}
   ```

### **Шаг 4: Проверить на iPhone**
1. Откройте Expo Go на iPhone
2. Перейдите в "Моя программа"
3. **Проверьте даты:** Должны быть в формате "10.02.2026" (НЕ "Invalid Date")
4. **Проверьте веса:** Должны быть в формате "3 × 8 (80)"

---

## ✅ КРИТЕРИИ УСПЕХА

### **В консоли браузера:**
- [x] Логи автозагрузки весов при добавлении упражнений
- [x] Логи сохранения полных данных при назначении тренировки
- [x] Нет ошибок JavaScript

### **На странице WorkoutDetails:**
- [x] Веса отображаются для всех упражнений
- [x] Даты выбраны для всех дней

### **На iPhone:**
- [x] Даты в формате "10.02.2026" (НЕ "Invalid Date")
- [x] Веса в формате "3 × 8 (80)"
- [x] Все упражнения отображаются правильно

---

## 🔍 ОТЛАДКА

### **Если даты все еще "Invalid Date":**
1. Откройте консоль браузера при назначении тренировки
2. Проверьте лог `📅 weekData.dates:`
3. Даты должны быть в формате `YYYY-MM-DD`:
   ```javascript
   {
     monday: "2026-02-10",
     tuesday: "2026-02-11",
     ...
   }
   ```
4. Если даты в формате `DD.MM.YYYY` → проблема в `WorkoutDetails.jsx`

### **Если веса не отображаются:**
1. Откройте консоль браузера при назначении тренировки
2. Проверьте лог `📦 weekData.days:`
3. Должно быть `7 дней` (или количество дней с упражнениями)
4. Если `0 дней` → проблема в `assignedWorkoutsService.js`

### **Проверка данных в Firebase:**
1. Откройте Firebase Console
2. Перейдите в Firestore Database
3. Откройте коллекцию `assignedWorkouts`
4. Найдите последнее назначение
5. Проверьте структуру:
   ```javascript
   {
     weekData: {
       weekNumber: 1,
       days: {
         monday: {
           exercises: [
             {
               name: "Жим лежа",
               exerciseData: {
                 weight: "80" // ✅ Вес должен быть здесь
               },
               numberSteps: 3,
               numberTimes: 8
             }
           ]
         }
       },
       dates: {
         monday: "2026-02-10" // ✅ Дата в формате YYYY-MM-DD
       }
     }
   }
   ```

---

## 📊 ИЗМЕНЕННЫЕ ФАЙЛЫ

1. **sport_base/src/firebase/services/assignedWorkoutsService.js**
   - Изменена функция `assignWeekToClient()`
   - Теперь сохраняет полные данные `weekData` (включая `days` с упражнениями)

2. **gym-calendar/app/(client)/workouts.tsx**
   - Улучшена функция `getExerciseDetails()`
   - Более надежный поиск весов в разных форматах

3. **sport_base/src/components/CreateWorkout/CreateWorkout.jsx**
   - Автозагрузка весов из clientBases (уже было сделано ранее)

---

## 🎯 ВАЖНО

### **Почему мы вернули сохранение weekData.days:**
- Раньше мы пытались оптимизировать и сохранять только ссылку на workout
- Но это создавало проблемы:
  - Старые данные из шаблона могли не содержать актуальные веса
  - Даты могли быть в неправильном формате
  - Если шаблон изменится, старые назначения тоже изменятся

### **Новый подход:**
- Сохраняем полные данные недели при назначении
- Это гарантирует что клиент получит именно те данные, которые были на момент назначения
- Даже если шаблон изменится, старые назначения останутся неизменными
- Размер данных: ~50KB на назначение (приемлемо для Firebase)

---

**ГОТОВО К ТЕСТИРОВАНИЮ!** 🎉

Теперь даты и веса должны отображаться правильно на iPhone.
