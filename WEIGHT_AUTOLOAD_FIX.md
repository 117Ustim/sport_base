# 🔧 ИСПРАВЛЕНИЕ: Автозагрузка весов (только для конкретных повторений)

**Дата:** 06.02.2026  
**Проблема:** Система подставляла "последний вес" если не было веса для текущих повторений

---

## 🐛 ЧТО БЫЛО НЕ ТАК

### **Старое поведение:**
```javascript
// Если нет веса для 8 повторений → брали последний вес
⚠️ Вес для 8 повторений не найден. Используем последний вес: 24 кг
```

**Проблема:**
- Упражнение "Наклоны одна нога" не имеет веса для 10 повторений
- Система подставляла вес 24 кг (последний записанный)
- **НО ЭТО НЕПРАВИЛЬНО!** Нужно оставить пустым

---

## ✅ НОВОЕ ПОВЕДЕНИЕ

### **Правило:**
**Вес подставляется ТОЛЬКО если он есть для конкретного количества повторений**

### **Примеры:**

**Пример 1: Вес есть**
- Упражнение: "Жим лежа"
- Количество повторений: 8
- В базе есть вес для 8 повторений: 80 кг
- **Результат:** ✅ Подставляется 80 кг

**Пример 2: Веса нет**
- Упражнение: "Наклоны одна нога"
- Количество повторений: 10
- В базе НЕТ веса для 10 повторений
- **Результат:** ℹ️ Оставляется пустым (НЕ подставляется другой вес)

**Пример 3: Изменили повторения**
- Упражнение: "Бицепс"
- Было: 3 × 8 (вес 20 кг)
- Изменили на: 3 × 12
- В базе есть вес для 12 повторений: 15 кг
- **Результат:** ✅ Автоматически обновляется на 15 кг

**Пример 4: Изменили повторения, но веса нет**
- Упражнение: "Трицепс"
- Было: 3 × 8 (вес 15 кг)
- Изменили на: 3 × 15
- В базе НЕТ веса для 15 повторений
- **Результат:** ℹ️ Вес убирается (оставляется пустым)

---

## 🔧 ЧТО ИСПРАВЛЕНО

### **1. Функция `onSelectExercise()` - при добавлении упражнения**

**Было:**
```javascript
// Если нет веса для текущих повторений → брали последний вес
const weights = Object.values(exercise.data).filter(w => w && w !== '');
if (weights.length > 0) {
  const lastWeight = weights[weights.length - 1];
  newExercise.exerciseData = { weight: lastWeight };
}
```

**Стало:**
```javascript
// Если нет веса для текущих повторений → оставляем пустым
const weightByReps = exercise.data[newExercise.numberTimes - 1];
if (weightByReps && weightByReps !== '' && weightByReps !== '—') {
  newExercise.exerciseData = { weight: weightByReps };
} else {
  // НЕ подставляем никакой вес
  console.log('ℹ️ Вес не найден для X повторений (оставляем пустым)');
}
```

### **2. Функция `handleUpdateExercise()` - при изменении повторений**

**Добавлено:**
```javascript
// Если изменили количество повторений → автоматически обновляем вес
if (field === 'numberTimes' && ex.exerciseData) {
  const newReps = Number(value);
  const weightForNewReps = ex.exerciseData[newReps - 1];
  
  if (weightForNewReps && weightForNewReps !== '') {
    updated.exerciseData = { weight: weightForNewReps };
    console.log('✅ Вес обновлен');
  } else {
    // Убираем вес если его нет
    delete updated.exerciseData.weight;
    console.log('ℹ️ Вес убран');
  }
}
```

### **3. Функция `handleBulkChangeReps()` - при массовом изменении**

**Добавлено:**
```javascript
// При массовом изменении повторений → обновляем веса для всех упражнений
const weightForNewReps = exercise.exerciseData[reps - 1];
if (weightForNewReps && weightForNewReps !== '') {
  updated.exerciseData = { weight: weightForNewReps };
} else {
  delete updated.exerciseData.weight;
}
```

---

## 🧪 КАК ПРОТЕСТИРОВАТЬ

### **Тест 1: Добавление упражнения с весом**
1. Откройте sport_base → Создать тренировку
2. Добавьте "Жим лежа" (есть вес для 8 повторений)
3. **Ожидаемый лог:**
   ```
   ✅ Автозагрузка веса для "Жим лежа": 80 кг (8 повторений)
   ```

### **Тест 2: Добавление упражнения без веса**
1. Добавьте "Наклоны одна нога" (нет веса для 8 повторений)
2. **Ожидаемый лог:**
   ```
   ℹ️ Вес для "Наклоны одна нога" не найден для 8 повторений (оставляем пустым)
   ```
3. **Проверка:** Вес должен быть пустым (НЕ 24 кг или другой)

### **Тест 3: Изменение повторений (вес есть)**
1. Добавьте "Бицепс" (3 × 8, вес 20 кг)
2. Измените на 3 × 12
3. **Ожидаемый лог:**
   ```
   ✅ Вес обновлен для "Бицепс": 15 кг (12 повторений)
   ```

### **Тест 4: Изменение повторений (веса нет)**
1. Добавьте "Трицепс" (3 × 8, вес 15 кг)
2. Измените на 3 × 15
3. **Ожидаемый лог:**
   ```
   ℹ️ Вес убран для "Трицепс" (нет веса для 15 повторений)
   ```
4. **Проверка:** Вес должен исчезнуть

### **Тест 5: Массовое изменение повторений**
1. Добавьте несколько упражнений
2. Используйте кнопку "Изменить повторения для всех" → 12
3. **Проверка:** 
   - Упражнения с весом для 12 повторений → вес обновится
   - Упражнения без веса для 12 повторений → вес исчезнет

---

## ✅ КРИТЕРИИ УСПЕХА

### **В консоли браузера:**
- [x] НЕТ логов "⚠️ Используем последний вес"
- [x] Есть логи "✅ Автозагрузка веса" (только когда вес найден)
- [x] Есть логи "ℹ️ Вес не найден" (когда веса нет)

### **На странице WorkoutDetails:**
- [x] Упражнения с весом показывают вес: "3 × 8 (80)"
- [x] Упражнения без веса НЕ показывают вес: "3 × 8"

### **На iPhone:**
- [x] Упражнения с весом: "3 × 8 (80)"
- [x] Упражнения без веса: "3 × 8" (без скобок)

---

## 📊 ИЗМЕНЕННЫЕ ФАЙЛЫ

1. **sport_base/src/components/CreateWorkout/CreateWorkout.jsx**
   - Функция `onSelectExercise()` - убрана логика "последнего веса"
   - Функция `handleUpdateExercise()` - добавлено автообновление веса
   - Функция `handleBulkChangeReps()` - добавлено автообновление весов

---

## 🎯 ВАЖНО

### **Старые тренировки:**
Если вы уже назначили тренировку ДО этого исправления:
1. Удалите старую тренировку на iPhone
2. Создайте тренировку ЗАНОВО в sport_base
3. Назначьте её клиенту
4. Проверьте на iPhone

### **Логика работы:**
```
Количество повторений = 8
↓
Ищем вес в clientBases[7] (индекс = 8 - 1)
↓
Если найден → подставляем
Если НЕ найден → оставляем пустым (НЕ ищем другой вес!)
```

---

**ГОТОВО К ТЕСТИРОВАНИЮ!** 🎉

Теперь веса подставляются ТОЛЬКО для конкретных повторений.
