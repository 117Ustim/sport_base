# 🗄️ ЭТАП 2: Audit Firebase Data Modeling & Architecture

**Дата:** 06.02.2026  
**Проекты:** sport_base (React Web) + gym-calendar (React Native)  
**Статус:** ✅ Завершен

---

## 📋 Цели этапа

1. Анализ структуры коллекций Firestore
2. Проверка денормализации данных
3. Оценка масштабируемости (лимиты записи, размеры документов)
4. Выявление избыточных "джойнов" на клиенте

---

## 🔍 Анализ структуры коллекций

### ✅ Хорошо спроектированные коллекции

#### 1. **clients** (Основная коллекция клиентов)
```
clients/{clientId}
├── profile: { name, surname, gymId, capacity, attented, ... }
├── attendance/ (subcollection)
│   └── {date}: { date, time, timestamp }
└── selfPurchases/ (subcollection)
    └── {purchaseId}: { date, trainings, amount }
```

**✅ Плюсы:**
- Использование subcollections для attendance — правильный подход
- Каждое посещение = отдельный документ (легко добавлять/удалять)
- Профиль клиента в одном месте

**⚠️ Минусы:**
- `capacity` и `attented` хранятся в profile — требуют обновления при каждом посещении
- Нет индексов для быстрого поиска по gymId или isActive

---

#### 2. **clientBases** (Базы упражнений клиентов)
```
clientBases/{clientId}
├── exercises/ (subcollection)
│   └── {exerciseId}: { name, categoryId, data: { [columnId]: weight } }
└── metadata/ (subcollection)
    └── settings: { columns: [...] }
```

**✅ Плюсы:**
- Subcollections для упражнений — масштабируемо
- Метаданные отдельно от упражнений

**⚠️ Минусы:**
- `data: { [columnId]: weight }` — динамическая структура, сложно индексировать
- При добавлении нового веса нужно обновлять весь объект `data`

---

#### 3. **statistics** (Статистика по залам)
```
statistics/{gymId}/daily/{date}
└── { trainedTotal, trainedTotalCost, trainedPersonal, ... }
```

**✅ Плюсы:**
- Subcollection по датам — отличное решение для временных рядов
- Легко запрашивать статистику за период

**⚠️ Потенциальная проблема:**
- При массовых отметках посещений (50+ клиентов одновременно) возможны конфликты записи в один документ `{date}`
- **Решение:** Использовать `increment()` вместо `setDoc()` с merge

---

### ⚠️ Проблемные коллекции

#### 1. **workouts** (Шаблоны тренировок)
```
workouts/{workoutId}
├── name: string
├── clientId: string
├── weeks: Array<Week>  ⚠️ БОЛЬШОЙ МАССИВ
│   └── Week:
│       ├── weekNumber: number
│       ├── dates: { [dayKey]: string }
│       └── days: { [dayKey]: DayData }
│           └── DayData:
│               └── exercises: Array<Exercise>  ⚠️ ВЛОЖЕННЫЙ МАССИВ
```

**❌ Критические проблемы:**

1. **Размер документа:**
   - Тренировка на 8 недель × 7 дней × 10 упражнений = **560 упражнений в одном документе**
   - Каждое упражнение содержит: name, sets, reps, weight, exerciseData, categoryId
   - **Риск превышения лимита 1 MB на документ**

2. **Неэффективные обновления:**
   - При изменении одного упражнения перезаписывается весь массив `weeks`
   - Firestore не поддерживает обновление элемента массива по индексу
   - Нужно загрузить весь документ → изменить → сохранить обратно

3. **Избыточная загрузка:**
   - Клиенту нужна только одна неделя, но загружается вся тренировка (все 8 недель)

**💡 Рекомендация:**
```
workouts/{workoutId}
├── name, clientId, createdAt
└── weeks/ (subcollection)  ✅ ЛУЧШЕ
    └── {weekNumber}
        ├── weekNumber: number
        ├── dates: { ... }
        └── days: { ... }
```

**Преимущества:**
- Каждая неделя = отдельный документ (до 1 MB на неделю)
- Обновление одной недели не затрагивает другие
- Клиент загружает только нужную неделю

---

#### 2. **assignedWorkouts** (Назначенные тренировки)
```
assignedWorkouts/{assignmentId}
├── clientId, userId, workoutId, weekNumber
├── weekData: { ...копия недели из workouts }  ⚠️ ДУБЛИРОВАНИЕ
└── status: 'new' | 'viewed' | 'completed'
```

**❌ Критические проблемы:**

1. **Дублирование данных:**
   - `weekData` полностью копируется из `workouts`
   - При изменении тренировки в `workouts` нужно обновлять все `assignedWorkouts`
   - **Пример:** Тренер исправил вес в упражнении → нужно обновить в 10+ местах

2. **Несогласованность данных:**
   - Старый код: `weekData` хранится в `assignedWorkouts`
   - Новый код: `weekData` загружается из `workouts` (JOIN)
   - **Результат:** Два разных источника правды

3. **Избыточные JOIN-запросы:**
   ```typescript
   // Текущий код в AssignedWorkoutsService.ts
   const assignment = await getDoc(assignmentRef);
   if (!assignment.weekData) {
       // Дополнительный запрос к workouts
       const workout = await getDoc(workoutRef);  ⚠️ JOIN
       assignment.weekData = workout.weeks[weekNumber];
   }
   ```

**💡 Рекомендация:**
```
assignedWorkouts/{assignmentId}
├── clientId, userId
├── workoutId  ✅ ТОЛЬКО ССЫЛКА
├── weekNumber  ✅ ТОЛЬКО НОМЕР
└── status, assignedAt
```

**Преимущества:**
- Нет дублирования данных
- Один источник правды (workouts)
- При изменении тренировки все клиенты видят актуальные данные
- Меньше размер документа

**Недостатки:**
- Требуется JOIN при загрузке (но это уже реализовано в новом коде)

---

#### 3. **Дублирование весов упражнений**

**Проблема:** Вес упражнения хранится в **3 местах:**

1. `workouts/{workoutId}/weeks[]/days[]/exercises[]/exerciseData.weight`
2. `assignedWorkouts/{assignmentId}/weekData/days[]/exercises[]/exerciseData.weight`
3. `clientBases/{clientId}/exercises/{exerciseId}/data[columnId]`

**Код из AssignedWorkoutsService.ts:**
```typescript
// 1. Обновляем в assignedWorkouts
await updateDoc(assignmentRef, { weekData: updatedWeekData });

// 2. Обновляем в workouts
await updateDoc(workoutRef, { weeks });

// 3. Обновляем в clientBases
await ClientBaseService.updateExerciseWeight(...);
```

**❌ Проблемы:**
- Три записи в Firebase вместо одной
- Риск несогласованности (если одна запись упадёт)
- Сложная логика синхронизации

**💡 Рекомендация:**
- Хранить веса только в `clientBases` (единственный источник правды)
- В `workouts` хранить только шаблон упражнения (name, sets, reps)
- При отображении тренировки загружать актуальные веса из `clientBases`

---

## 📊 Оценка масштабируемости

### Лимиты Firestore и риски

| Лимит | Значение | Текущее использование | Риск |
|-------|----------|----------------------|------|
| **Запись в 1 документ** | 1/сек | `statistics/{gymId}/daily/{date}` обновляется при каждом посещении | 🔴 **ВЫСОКИЙ** |
| **Размер документа** | 1 MB | `workouts` с 8 неделями может достичь 500-800 KB | 🟡 **СРЕДНИЙ** |
| **Subcollection depth** | 100 уровней | Используется 2 уровня (clients/{id}/attendance) | 🟢 **НИЗКИЙ** |
| **Batch write** | 500 операций | Каскадное удаление клиента (~50-100 операций) | 🟢 **НИЗКИЙ** |
| **Reads/день (free)** | 50,000 | ~6,000/день (100 клиентов) | 🟢 **НИЗКИЙ** |
| **Writes/день (free)** | 20,000 | ~6,000/день (100 клиентов) | 🟢 **НИЗКИЙ** |

---

### 🔴 Критический риск: Конфликты записи в statistics

**Сценарий:**
- 50 клиентов одновременно отмечаются в зале
- Все обновляют один документ: `statistics/{gymId}/daily/2026-02-06`
- Firestore лимит: **1 запись/сек на документ**

**Текущий код (StatisticsService.ts):**
```typescript
const statsRef = doc(db, 'statistics', gymId, 'daily', date);
const statsSnap = await getDoc(statsRef);

if (!statsSnap.exists()) {
    await setDoc(statsRef, { trainedTotal: 1, ... });
} else {
    const current = statsSnap.data();
    await updateDoc(statsRef, {
        trainedTotal: current.trainedTotal + 1  ⚠️ RACE CONDITION
    });
}
```

**Проблема:**
- Два клиента одновременно читают `trainedTotal: 10`
- Оба записывают `trainedTotal: 11`
- **Результат:** Потеря одного посещения

**💡 Решение: Использовать increment()**
```typescript
import { increment } from 'firebase/firestore';

await updateDoc(statsRef, {
    trainedTotal: increment(1),  ✅ АТОМАРНАЯ ОПЕРАЦИЯ
    trainedTotalCost: increment(price)
});
```

**Преимущества:**
- Атомарная операция (нет race conditions)
- Не нужно читать документ перед записью (экономия reads)
- Firestore сам обрабатывает конкурентные обновления

---

### 🟡 Средний риск: Размер документа workouts

**Расчёт размера:**
```
Тренировка на 8 недель:
- 8 недель × 7 дней = 56 дней
- 56 дней × 10 упражнений = 560 упражнений
- Каждое упражнение: ~200 байт (name, sets, reps, weight, categoryId, exerciseData)
- Итого: 560 × 200 = 112 KB

Но если добавить:
- Комментарии к упражнениям
- История изменений
- Дополнительные поля
→ Может вырасти до 500-800 KB
```

**Лимит Firestore:** 1 MB на документ

**💡 Рекомендация:**
- Разбить `weeks` на subcollection (каждая неделя = отдельный документ)
- Это даст 8 MB на тренировку (8 недель × 1 MB)

---

## 🔄 Избыточные JOIN-запросы на клиенте

### Проблема 1: Загрузка assignedWorkouts

**Текущий код (AssignedWorkoutsService.ts):**
```typescript
// 1. Загружаем все assignments
const assignments = await getDocs(query(assignmentsRef, where('userId', '==', userId)));

// 2. Для каждого assignment загружаем workout (если weekData нет)
for (const assignment of assignments) {
    if (!assignment.weekData) {
        const workout = await getDoc(workoutRef);  ⚠️ N+1 PROBLEM
        assignment.weekData = workout.weeks[weekNumber];
    }
}
```

**Проблема:**
- Если у клиента 10 назначенных тренировок → **11 запросов** (1 + 10)
- Это классическая проблема N+1

**✅ Оптимизация (уже реализована):**
```typescript
// Собираем все workoutId
const workoutsToFetch = new Map();
assignments.forEach(a => {
    if (!a.weekData) workoutsToFetch.set(a.workoutId, ...);
});

// Загружаем все workouts параллельно
const workoutPromises = Array.from(workoutsToFetch.keys()).map(id => 
    getDoc(doc(db, 'workouts', id))
);
await Promise.all(workoutPromises);  ✅ ПАРАЛЛЕЛЬНО
```

**Результат:**
- Вместо 11 последовательных запросов → **2 параллельных** (assignments + workouts)
- Время загрузки сократилось в ~5 раз

---

### Проблема 2: Загрузка клиентов с тренировками

**Сценарий:** Админ открывает список клиентов и хочет видеть, у кого есть активные тренировки

**Текущий подход:**
```typescript
// 1. Загружаем всех клиентов
const clients = await getDocs(collection(db, 'clients'));  // 100 клиентов

// 2. Для каждого клиента загружаем assignedWorkouts
for (const client of clients) {
    const assignments = await getDocs(
        query(assignmentsRef, where('clientId', '==', client.id))
    );  ⚠️ 100 ЗАПРОСОВ
}
```

**Проблема:**
- 100 клиентов → **101 запрос** (1 + 100)

**💡 Рекомендация:**
```typescript
// 1. Загружаем всех клиентов
const clients = await getDocs(collection(db, 'clients'));

// 2. Загружаем ВСЕ assignedWorkouts одним запросом
const allAssignments = await getDocs(collection(db, 'assignedWorkouts'));

// 3. Группируем на клиенте
const assignmentsByClient = {};
allAssignments.forEach(a => {
    if (!assignmentsByClient[a.clientId]) {
        assignmentsByClient[a.clientId] = [];
    }
    assignmentsByClient[a.clientId].push(a);
});
```

**Результат:**
- Вместо 101 запроса → **2 запроса**
- Но: Загружаем все assignments (даже для клиентов, которые не на экране)

**Альтернатива:** Добавить поле `hasActiveWorkout: boolean` в `clients/{id}/profile`
- Обновлять при назначении/удалении тренировки
- Фильтровать клиентов на клиенте без дополнительных запросов

---

### Проблема 3: Обновление упражнения

**Текущий код (AssignedWorkoutsService.updateExerciseData):**
```typescript
// 1. Загружаем assignment
const assignment = await getDoc(assignmentRef);

// 2. Если weekData нет, загружаем workout
if (!assignment.weekData) {
    const workout = await getDoc(workoutRef);  ⚠️ JOIN
}

// 3. Обновляем weekData в assignment
await updateDoc(assignmentRef, { weekData: updatedWeekData });

// 4. Обновляем workout
await updateDoc(workoutRef, { weeks: updatedWeeks });

// 5. Сохраняем в историю
await ExerciseHistoryService.addHistoryEntry(...);

// 6. Обновляем clientBases
await ClientBaseService.updateExerciseWeight(...);
```

**Проблема:**
- **6 операций** для обновления одного веса
- 2 reads + 4 writes = **6 Firebase операций**

**💡 Рекомендация:**
- Хранить веса только в `clientBases`
- В `workouts` хранить только шаблон
- При отображении загружать актуальные веса из `clientBases`
- **Результат:** 1 read + 1 write = **2 операции** (экономия 67%)

---

## 💰 Оценка стоимости Firebase

### Текущая архитектура (100 клиентов)

**Ежедневные операции:**

| Операция | Количество | Reads | Writes |
|----------|-----------|-------|--------|
| Отметка посещений | 50 клиентов | 50 (statistics) | 100 (attendance + profile) + 50 (statistics) |
| Загрузка календаря | 100 клиентов | 100 (clients) + 100 (attendance) | 0 |
| Загрузка тренировок | 50 клиентов | 50 (assignments) + 50 (workouts) | 0 |
| Обновление упражнения | 20 клиентов | 40 (assignment + workout) | 80 (assignment + workout + clientBase + history) |

**Итого в день:**
- **Reads:** 50 + 200 + 100 + 40 = **390 reads**
- **Writes:** 150 + 80 = **230 writes**

**Итого в месяц:**
- **Reads:** 390 × 30 = **11,700 reads**
- **Writes:** 230 × 30 = **6,900 writes**

**Стоимость (бесплатный план):**
- Reads: 50,000/день ✅ (используется 0.78%)
- Writes: 20,000/день ✅ (используется 1.15%)

---

### Оптимизированная архитектура (прогноз)

**Изменения:**
1. Убрать дублирование `weekData` в `assignedWorkouts` → **-50% writes**
2. Использовать `increment()` в statistics → **-50 reads/день**
3. Хранить веса только в `clientBases` → **-60 writes/день**

**Итого в день:**
- **Reads:** 390 - 50 = **340 reads** (-13%)
- **Writes:** 230 - 115 - 60 = **55 writes** (-76%)

**Экономия в месяц:**
- **Reads:** 1,500 reads (-13%)
- **Writes:** 5,250 writes (-76%)

**При росте до 500 клиентов:**
- **Текущая:** ~35,000 writes/месяц (выход за бесплатный план)
- **Оптимизированная:** ~8,250 writes/месяц (в пределах бесплатного плана)

---

## 🎯 Рекомендации по приоритетам

### 🔴 Критический приоритет (сделать немедленно)

1. **Использовать `increment()` в StatisticsService**
   - **Проблема:** Race conditions при массовых отметках
   - **Решение:** Заменить `updateDoc` на `increment()`
   - **Время:** 30 минут
   - **Эффект:** Устранение потери данных

2. **Добавить limit() в запросы assignedWorkouts**
   - **Проблема:** Загружаются все тренировки клиента (даже старые)
   - **Решение:** `query(assignmentsRef, limit(10))`
   - **Время:** 15 минут
   - **Эффект:** -80% reads при загрузке тренировок

---

### 🟡 Высокий приоритет (сделать в ближайшее время)

3. **Убрать дублирование weekData в assignedWorkouts**
   - **Проблема:** Несогласованность данных, избыточные writes
   - **Решение:** Хранить только `workoutId` + `weekNumber`
   - **Время:** 2-3 часа (с миграцией данных)
   - **Эффект:** -50% writes, один источник правды

4. **Разбить workouts.weeks на subcollection**
   - **Проблема:** Риск превышения 1 MB на документ
   - **Решение:** `workouts/{id}/weeks/{weekNumber}`
   - **Время:** 4-5 часов (с миграцией данных)
   - **Эффект:** Масштабируемость до 8 MB на тренировку

---

### 🟢 Средний приоритет (можно отложить)

5. **Хранить веса только в clientBases**
   - **Проблема:** Дублирование в 3 местах
   - **Решение:** Загружать актуальные веса при отображении
   - **Время:** 6-8 часов (большой рефакторинг)
   - **Эффект:** -60% writes, упрощение логики

6. **Добавить поле hasActiveWorkout в clients**
   - **Проблема:** N+1 запросы при загрузке списка клиентов
   - **Решение:** Денормализация для быстрого фильтра
   - **Время:** 2 часа
   - **Эффект:** -100 reads при загрузке списка

---

## 📋 Следующие этапы

**Этап 3:** Audit Network & Subscriptions
- Проверка `onSnapshot` подписок на утечки памяти
- Анализ cleanup функций в useEffect
- Поиск мест, где real-time можно заменить на `.get()`

**Этап 4:** Audit Performance & Render Cycles
- Анализ `useEffect` зависимостей
- Проверка мемоизации
- Поиск лишних ре-рендеров

---

**Готов к следующему этапу? 🚀**
